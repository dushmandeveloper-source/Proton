# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Local environment overrides (e.g. VITE_API_BASE_URL) — see .env.example
.env
.env.local
.env.development
.env.development.local
.env.production.local
!.env.example
# .env.production is committed on purpose — it sets VITE_API_BASE_URL to the
# hosted backend so Vercel builds pick it up without dashboard config.

# Playwright verification screenshots (dev artifacts, not source)
shots/
shot-*.png

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
