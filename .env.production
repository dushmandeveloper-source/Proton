# Committed on purpose: Vercel builds this project from git, and Vite bakes
# VITE_ vars in at build time — so the hosted backend URL needs to live here
# rather than only in a gitignored .env. See .env.example for local dev.
VITE_API_BASE_URL=https://dushman-002-site3.gtempurl.com
